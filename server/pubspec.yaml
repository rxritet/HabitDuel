name: habitduel_server
description: HabitDuel backend server built with Dart Shelf.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  postgres: ^3.3.0
  dart_jsonwebtoken: ^2.12.0
  crypto: ^3.0.3
  dotenv: ^4.1.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
