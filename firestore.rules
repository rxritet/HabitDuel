rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Development rules: allow the app to mirror data into Firestore.
    // Tighten these rules again before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}