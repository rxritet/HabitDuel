rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Transitional rules: any authenticated Firebase user can access data.
    // The app currently signs in anonymously at startup.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}